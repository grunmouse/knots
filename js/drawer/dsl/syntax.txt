<MAIN> := PROG <EOF>;

PROG := PROG S;
PROG := S;

S := REP;
S := STAT;

STAT := command;
STAT := command ARGS;

REP := { PROG } [ EXP ];
REP := { } [ EXP ];

ARGS := ARGS , SUBARGS;
ARGS := SUBARGS;

SUBARGS := SPREED;
SUBARGS := ARG;

SPREED := spreed varname;
ARG := EXP;

EXP := SUMM;

SUMM := SIGNED;
SIGNED := ots SUMMAND;

SUMM := SUMM ots SUMMAND;
SUMM := SUMMAND;

SUMMAND := MULT;
MULT := RATIONAL;
RATIONAL := otm MULTIPLIER;

MULT := MULTIPLIER;
MULT := MULT otm MULTIPLIER;

MULTIPLIER := POW;

POW := BASE otp POWER;
POW := OPERAND;

BASE := OPERAND;
POWER := OPERAND;

OPERAND := ( EXP );
OPERAND := FUNCALL;
OPERAND := value;
OPERAND := varname;

FUNCALL := funname ( ARGS );
FUNCALL := funname ( );